general:

machine:
  java:
    version: openjdk7
  timezone:
    Asia/Tokyo

dependencies:
  pre:
    - export DEBIAN_FRONTEND=noninteractive
    - sudo apt-get update
    - sudo apt-get -y install handbrake*
