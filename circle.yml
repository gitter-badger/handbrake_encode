general:

machine:
  java:
    version: openjdk7
  timezone:
    Asia/Tokyo

dependencies:
  pre:
    - chmod +x ./gradlew
    - uname -r

test:
  pre:
    - sudo chmod 666 /etc/apt/sources.list
    - sudo echo 'deb http://ppa.launchpad.net/stebbins/handbrake-releases/ubuntu precise main' >> /etc/apt/sources.list
    - sudo apt-get update
    - sudo apt-get -y install handbrake-cli
    - which handbrake-cli
    - sudo handbrake-cli --help
    
    
    